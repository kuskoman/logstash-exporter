logstash:
  instances:
    - url: "http://localhost:9600"
  timeout: 3s
server:
  host: "127.0.0.1"
  port: 9183
  tls_server_config:
    cert_file: "fixtures/https/server.crt" # Note: paths are relative to where exporter is run
    key_file: "fixtures/https/server.key"
logging:
  level: "info"
